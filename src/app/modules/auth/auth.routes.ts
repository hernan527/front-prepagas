import { Routes } from '@angular/router';
import { INTERNAL_ROUTES } from '../../data/constants/routes';
import { SigninComponent } from './pages/signin/signin.component';


export const routes: Routes = [{ path: '', component: SigninComponent }];
